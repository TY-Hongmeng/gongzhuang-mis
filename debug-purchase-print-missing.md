# [OPEN] purchase-print-missing

## 现象
- 采购管理 -> 采购审批 -> 打印审批清单
- 打印页中 `投产单位`、`申请日期`、`提交人` 等字段仍然出现缺失
- 用户要求顺序是：先尽量铺满页面，再做合并，且不能丢内容

## 影响
- 打印清单与页面表格展示不一致
- 审批纸质单存在缺字段风险

## 当前假设
- 假设 1：`filteredData` 中这些字段本身有值，但 `printRows` 生成后被错误覆盖或未继承完全
- 假设 2：分页后页内 `rowspan` 计算条件错误，导致首行单元格未输出
- 假设 3：部分字段在打印流程里使用了与列表展示不同的来源字段或 fallback 顺序
- 假设 4：连续空值组的“补齐逻辑”仍然会在组切换或分页边界处断链

## 下一步
- 先对打印函数增加运行时日志，记录打印前原始行、补齐后行、分页结果、首行单元格输出条件
- 收到证据后再做最小修复
